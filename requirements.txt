# ============================================================
# Factoryskills - All Dependencies
# ============================================================

# === Web Platform (FastAPI + ASGI) ===
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
jinja2>=3.1.0
python-multipart>=0.0.9

# === Database ===
aiosqlite>=0.20.0

# === Authentication ===
bcrypt>=4.2.0
itsdangerous>=2.2.0

# === Environment config ===
python-dotenv>=1.0.0

# === Process management (Excel zombie cleanup) ===
psutil>=6.0.0

# === Skills: order_docs ===
openpyxl>=3.1.0
# HTML/CSS + Playwright ベースの PDF 生成 (ReportLab は v2.1 で廃止)
# Playwright は Chromium バイナリの別途インストールが必要:
#   python -m playwright install chromium
playwright>=1.40.0

# === Skills: construction_cost ===
pandas>=2.2.0
# pywin32 は Windows でのみインストールする（Linux/macOS では skip）
# Linux サーバ（さくらVPS等）で `pip install -r requirements.txt` が
# 失敗しないよう PEP 508 environment marker でガードしている。
pywin32>=306; sys_platform == 'win32'
pypdf>=6.0.0
PyMuPDF>=1.24.0

# === Skills: qualifications (資格者証管理) ===
# Gemini API 公式 SDK — gemini-2.5-flash で資格者証 PDF/画像から
# 構造化 JSON を抽出する OCR に使用。GEMINI_API_KEY (.env) が必須。
google-genai>=1.0.0
# 画像前処理（リサイズ・回転）に使用。
Pillow>=10.4.0
# PDF を画像化してサムネイル / Gemini 入力を作る。
# 注: pdf2image は OS の poppler-utils 依存。
#   Ubuntu/Debian: sudo apt install -y poppler-utils
#   Windows      : poppler-windows を PATH に通す
pdf2image>=1.17.0

# === KGK SSO 統合 (ADR-003) ===
# kgk-redis (127.0.0.1:6379) と共有するワンタイムチケットストアに使う。
# Phase 2a 以降で web_app/core/redis_client.py が参照する。
redis>=5.0.0

# === Optional (CLI monitoring - not required for web) ===
# watchdog>=4.0.0
# plyer>=2.1.0
